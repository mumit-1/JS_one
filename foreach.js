// foreach wroks on and works like map but doesn't have return . i mean doesnt give anything in return.

const array = [1, 2, 3, 3, 2, 4, 54, 5];

const result = array.forEach((n) => n * 2);
console.log(result); // output : undefined
