let isLeader = false;

if(!!isLeader){
    console.log('dara');
}
else{
    console.log('bosh');
}
// example 1. if(isLeader) 
// it is set in check mode of true in auto (isLeader == true) **************************

// example 2 . if(!isLeader)
// it is set in reverse which is check mode of false (isLeader == false) **************************

// example 3 . if(!!isLeader)
// it is set mode of  reversing example 2 . for understanding , (!(!isLeader)). and it is same as same as example one for work . ******************